const APP_TAG = 'bolt-task';

const APP_NAME = 'bolt-task';

const DEFAULT_LANG = 'en';

const GLOBAL_DELAY = 200;

const NODE_ENV = import.meta.env.VITE_NODE_ENV;

const CONFIG = {
  APP_TAG,
  APP_NAME,
  NODE_ENV,
  DEFAULT_LANG,
  GLOBAL_DELAY,
};

export default CONFIG;
